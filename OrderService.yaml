apiVersion: apps/v1
kind: Deployment
metadata:
  name: orderservice-development
  labels:
    app: orderservice
spec:
    replicas: 1
    selector:
      matchLabels:
        app: orderservice
    template:
      metadata: 
        labels:
          app: orderservice
      spec:
        containers:
          - name: orderservice
            image: tarar290190/oderservice:v1.0
            ports:
            - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: order-service
spec:
  type: NodePort
  selector:
    app: orderservice
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      NodePort: 31000
  
      