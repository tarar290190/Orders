apiVersion: apps/v1
kind: Deployment
metadata:
  name: orderservicedevelopment
  labels:
    app: orderservice
spec:
    replicas: 1
    selector:
      matchLabels:
        app: orderservice
    template:
      metadata: 
        labels:
          app: orderservice
        spec:
          containers:
            - name: orderservice
              image: tarar290190/oderservice:v1.0
              ports:
              - containerPorts: 15001

---
apiVersion: v1
kind: Service
metadata:
  name: orderservice
spec:
  selector:
    app: orderservice
  ports:
    - protocol: TCP
      port: 80
      targetPort: 15001 
      nodePort: 80 